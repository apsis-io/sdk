{
  "name": "periapsis-magicseam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Magic-seam (ADR-0028) provider for a genuinely non-WASM TypeScript program (Node.js/Bun) - the revived MSK1 protocol, mirroring sdk/go/magicseam. NOT for use inside a dwarf/WASM component - see ../periapsis/magic.ts for that.",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "7.0.1-rc"
  },
  "scripts": {
    "test": "bun test"
  }
}
