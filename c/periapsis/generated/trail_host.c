// Generated by `wit-bindgen` 0.59.0. DO NOT EDIT!
#include "trail_host.h"
#include <stdlib.h>
#include <string.h>

// Imported Functions from `periapsis:component/identity@0.1.0`

__attribute__((__import_module__("periapsis:component/identity@0.1.0"), __import_name__("get")))
extern void __wasm_import_periapsis_component_identity_get(uint8_t *);

// Imported Functions from `periapsis:component/config@0.1.0`

__attribute__((__import_module__("periapsis:component/config@0.1.0"), __import_name__("get")))
extern void __wasm_import_periapsis_component_config_get(uint8_t *, size_t, uint8_t *);

// Imported Functions from `periapsis:component/log@0.1.0`

__attribute__((__import_module__("periapsis:component/log@0.1.0"), __import_name__("emit")))
extern void __wasm_import_periapsis_component_log_emit(int32_t, int32_t, uint8_t *, size_t, uint8_t *, size_t, uint8_t *, size_t, uint8_t *);

// Imported Functions from `periapsis:component/metrics@0.1.0`

__attribute__((__import_module__("periapsis:component/metrics@0.1.0"), __import_name__("increment-counter")))
extern void __wasm_import_periapsis_component_metrics_increment_counter(uint8_t *, size_t, int64_t, uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("periapsis:component/metrics@0.1.0"), __import_name__("record-gauge")))
extern void __wasm_import_periapsis_component_metrics_record_gauge(uint8_t *, size_t, double, uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("periapsis:component/metrics@0.1.0"), __import_name__("record-histogram")))
extern void __wasm_import_periapsis_component_metrics_record_histogram(uint8_t *, size_t, double, uint8_t *, size_t, uint8_t *);

// Imported Functions from `periapsis:component/status@0.1.0`

__attribute__((__import_module__("periapsis:component/status@0.1.0"), __import_name__("notify")))
extern void __wasm_import_periapsis_component_status_notify(uint8_t *, size_t, uint8_t *, size_t, int32_t, int32_t, uint8_t *, size_t, int32_t, uint8_t *, size_t, uint8_t *, size_t, int32_t, int64_t, uint8_t *);

// Imported Functions from `periapsis:component/checkpoint@0.1.0`

__attribute__((__import_module__("periapsis:component/checkpoint@0.1.0"), __import_name__("requested")))
extern int32_t __wasm_import_periapsis_component_checkpoint_requested(void);

__attribute__((__import_module__("periapsis:component/checkpoint@0.1.0"), __import_name__("save")))
extern void __wasm_import_periapsis_component_checkpoint_save(uint8_t *, size_t);

__attribute__((__import_module__("periapsis:component/checkpoint@0.1.0"), __import_name__("load")))
extern void __wasm_import_periapsis_component_checkpoint_load(uint8_t *);

// Canonical ABI intrinsics

__attribute__((__weak__, __export_name__("cabi_realloc")))
void *cabi_realloc(void *ptr, size_t old_size, size_t align, size_t new_size) {
  (void) old_size;
  if (new_size == 0) return (void*) align;
  void *ret = realloc(ptr, new_size);
  if (!ret) abort();
  return ret;
}

// Helper Functions

void periapsis_component_types_attribute_free(periapsis_component_types_attribute_t *ptr) {
  trail_host_string_free(&ptr->key);
  trail_host_string_free(&ptr->value);
}

void periapsis_component_identity_attribute_free(periapsis_component_identity_attribute_t *ptr) {
  periapsis_component_types_attribute_free(ptr);
}

void trail_host_option_string_free(trail_host_option_string_t *ptr) {
  if (ptr->is_some) {
    trail_host_string_free(&ptr->val);
  }
}

void periapsis_component_identity_list_attribute_free(periapsis_component_identity_list_attribute_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    periapsis_component_identity_attribute_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      periapsis_component_identity_attribute_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void periapsis_component_identity_identity_info_free(periapsis_component_identity_identity_info_t *ptr) {
  trail_host_string_free(&ptr->component);
  trail_host_string_free(&ptr->instance);
  trail_host_string_free(&ptr->sdk_version);
  trail_host_option_string_free(&ptr->workload);
  trail_host_option_string_free(&ptr->namespace_);
  trail_host_option_string_free(&ptr->pod_name);
  trail_host_option_string_free(&ptr->pod_uid);
  trail_host_option_string_free(&ptr->pawn_name);
  trail_host_option_string_free(&ptr->node_name);
  periapsis_component_identity_list_attribute_free(&ptr->attributes);
}

void trail_host_list_u8_free(trail_host_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void trail_host_list_string_free(trail_host_list_string_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    trail_host_string_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      trail_host_string_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void periapsis_component_config_config_value_free(periapsis_component_config_config_value_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      trail_host_string_free(&ptr->val.text);
      break;
    }
    case 1: {
      break;
    }
    case 2: {
      break;
    }
    case 3: {
      break;
    }
    case 4: {
      break;
    }
    case 5: {
      trail_host_list_u8_free(&ptr->val.bytes);
      break;
    }
    case 6: {
      trail_host_list_string_free(&ptr->val.text_list);
      break;
    }
  }
}

void periapsis_component_config_option_config_value_free(periapsis_component_config_option_config_value_t *ptr) {
  if (ptr->is_some) {
    periapsis_component_config_config_value_free(&ptr->val);
  }
}

void periapsis_component_config_result_option_config_value_config_error_free(periapsis_component_config_result_option_config_value_config_error_t *ptr) {
  if (!ptr->is_err) {
    periapsis_component_config_option_config_value_free(&ptr->val.ok);
  } else {
  }
}

void periapsis_component_log_attribute_free(periapsis_component_log_attribute_t *ptr) {
  periapsis_component_types_attribute_free(ptr);
}

void periapsis_component_log_list_attribute_free(periapsis_component_log_list_attribute_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    periapsis_component_log_attribute_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      periapsis_component_log_attribute_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void periapsis_component_log_entry_free(periapsis_component_log_entry_t *ptr) {
  trail_host_option_string_free(&ptr->target);
  trail_host_string_free(&ptr->message);
  periapsis_component_log_list_attribute_free(&ptr->attributes);
}

void periapsis_component_log_result_void_log_error_free(periapsis_component_log_result_void_log_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
  }
}

void periapsis_component_metrics_attribute_free(periapsis_component_metrics_attribute_t *ptr) {
  periapsis_component_types_attribute_free(ptr);
}

void periapsis_component_metrics_list_attribute_free(periapsis_component_metrics_list_attribute_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    periapsis_component_metrics_attribute_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      periapsis_component_metrics_attribute_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void periapsis_component_metrics_labels_free(periapsis_component_metrics_labels_t *ptr) {
  periapsis_component_metrics_list_attribute_free(&ptr->values);
}

void periapsis_component_metrics_result_void_metric_error_free(periapsis_component_metrics_result_void_metric_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
  }
}

void periapsis_component_status_attribute_free(periapsis_component_status_attribute_t *ptr) {
  periapsis_component_types_attribute_free(ptr);
}

void periapsis_component_status_list_attribute_free(periapsis_component_status_list_attribute_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    periapsis_component_status_attribute_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      periapsis_component_status_attribute_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void trail_host_option_u64_free(trail_host_option_u64_t *ptr) {
  if (ptr->is_some) {
  }
}

void periapsis_component_status_report_free(periapsis_component_status_report_t *ptr) {
  trail_host_string_free(&ptr->component);
  trail_host_string_free(&ptr->instance);
  trail_host_option_string_free(&ptr->reason);
  trail_host_option_string_free(&ptr->message);
  periapsis_component_status_list_attribute_free(&ptr->attributes);
  trail_host_option_u64_free(&ptr->sequence);
}

void periapsis_component_status_result_void_status_error_free(periapsis_component_status_result_void_status_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
  }
}

void trail_host_option_list_u8_free(trail_host_option_list_u8_t *ptr) {
  if (ptr->is_some) {
    trail_host_list_u8_free(&ptr->val);
  }
}

void trail_host_string_set(trail_host_string_t *ret, const char*s) {
  ret->ptr = (uint8_t*) s;
  ret->len = strlen(s);
}

void trail_host_string_dup(trail_host_string_t *ret, const char*s) {
  ret->len = strlen(s);
  ret->ptr = (uint8_t*) cabi_realloc(NULL, 0, 1, ret->len * 1);
  memcpy(ret->ptr, s, ret->len * 1);
}

void trail_host_string_dup_n(trail_host_string_t *ret, const char*s, size_t len) {
  ret->len = len;
  ret->ptr = (uint8_t*) cabi_realloc(NULL, 0, 1, ret->len * 1);
  memcpy(ret->ptr, s, ret->len * 1);
}

void trail_host_string_free(trail_host_string_t *ret) {
  if (ret->len > 0) {
    free(ret->ptr);
  }
  ret->ptr = NULL;
  ret->len = 0;
}

// Component Adapters

void periapsis_component_identity_get(periapsis_component_identity_identity_info_t *ret) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(26*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_identity_get(ptr);
  trail_host_option_string_t option;
  switch ((int32_t) *((uint8_t*) (ptr + (6*sizeof(void*))))) {
    case 0: {
      option.is_some = false;
      break;
    }
    case 1: {
      option.is_some = true;
      option.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (7*sizeof(void*))))), (*((size_t*) (ptr + (8*sizeof(void*))))) };
      break;
    }
  }
  trail_host_option_string_t option0;
  switch ((int32_t) *((uint8_t*) (ptr + (9*sizeof(void*))))) {
    case 0: {
      option0.is_some = false;
      break;
    }
    case 1: {
      option0.is_some = true;
      option0.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (10*sizeof(void*))))), (*((size_t*) (ptr + (11*sizeof(void*))))) };
      break;
    }
  }
  trail_host_option_string_t option1;
  switch ((int32_t) *((uint8_t*) (ptr + (12*sizeof(void*))))) {
    case 0: {
      option1.is_some = false;
      break;
    }
    case 1: {
      option1.is_some = true;
      option1.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (13*sizeof(void*))))), (*((size_t*) (ptr + (14*sizeof(void*))))) };
      break;
    }
  }
  trail_host_option_string_t option2;
  switch ((int32_t) *((uint8_t*) (ptr + (15*sizeof(void*))))) {
    case 0: {
      option2.is_some = false;
      break;
    }
    case 1: {
      option2.is_some = true;
      option2.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (16*sizeof(void*))))), (*((size_t*) (ptr + (17*sizeof(void*))))) };
      break;
    }
  }
  trail_host_option_string_t option3;
  switch ((int32_t) *((uint8_t*) (ptr + (18*sizeof(void*))))) {
    case 0: {
      option3.is_some = false;
      break;
    }
    case 1: {
      option3.is_some = true;
      option3.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (19*sizeof(void*))))), (*((size_t*) (ptr + (20*sizeof(void*))))) };
      break;
    }
  }
  trail_host_option_string_t option4;
  switch ((int32_t) *((uint8_t*) (ptr + (21*sizeof(void*))))) {
    case 0: {
      option4.is_some = false;
      break;
    }
    case 1: {
      option4.is_some = true;
      option4.val = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (22*sizeof(void*))))), (*((size_t*) (ptr + (23*sizeof(void*))))) };
      break;
    }
  }
  *ret = (periapsis_component_identity_identity_info_t) {
    (trail_host_string_t) (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 0))), (*((size_t*) (ptr + sizeof(void*)))) },
    (trail_host_string_t) (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (2*sizeof(void*))))), (*((size_t*) (ptr + (3*sizeof(void*))))) },
    (trail_host_string_t) (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + (4*sizeof(void*))))), (*((size_t*) (ptr + (5*sizeof(void*))))) },
    (trail_host_option_string_t) option,
    (trail_host_option_string_t) option0,
    (trail_host_option_string_t) option1,
    (trail_host_option_string_t) option2,
    (trail_host_option_string_t) option3,
    (trail_host_option_string_t) option4,
    (periapsis_component_identity_list_attribute_t) (periapsis_component_identity_list_attribute_t) { (periapsis_component_identity_attribute_t*)(*((uint8_t **) (ptr + (24*sizeof(void*))))), (*((size_t*) (ptr + (25*sizeof(void*))))) },
  };
}

bool periapsis_component_config_get(trail_host_string_t *key, periapsis_component_config_option_config_value_t *ret, periapsis_component_config_config_error_t *err) {
  __attribute__((__aligned__(8)))
  uint8_t ret_area[(24+2*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_config_get((uint8_t *) (*key).ptr, (*key).len, ptr);
  periapsis_component_config_result_option_config_value_config_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      periapsis_component_config_option_config_value_t option;
      switch ((int32_t) *((uint8_t*) (ptr + 8))) {
        case 0: {
          option.is_some = false;
          break;
        }
        case 1: {
          option.is_some = true;
          periapsis_component_config_config_value_t variant;
          variant.tag = (int32_t) *((uint8_t*) (ptr + 16));
          switch ((int32_t) variant.tag) {
            case 0: {
              variant.val.text = (trail_host_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 24))), (*((size_t*) (ptr + (24+1*sizeof(void*))))) };
              break;
            }
            case 1: {
              variant.val.boolean = (int32_t) *((uint8_t*) (ptr + 24));
              break;
            }
            case 2: {
              variant.val.signed_ = *((int64_t*) (ptr + 24));
              break;
            }
            case 3: {
              variant.val.unsigned_ = (uint64_t) (*((int64_t*) (ptr + 24)));
              break;
            }
            case 4: {
              variant.val.float_ = *((double*) (ptr + 24));
              break;
            }
            case 5: {
              variant.val.bytes = (trail_host_list_u8_t) { (uint8_t*)(*((uint8_t **) (ptr + 24))), (*((size_t*) (ptr + (24+1*sizeof(void*))))) };
              break;
            }
            case 6: {
              variant.val.text_list = (trail_host_list_string_t) { (trail_host_string_t*)(*((uint8_t **) (ptr + 24))), (*((size_t*) (ptr + (24+1*sizeof(void*))))) };
              break;
            }
          }

          option.val = variant;
          break;
        }
      }

      result.val.ok = option;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 8));
      break;
    }
  }
  if (!result.is_err) {
    *ret = result.val.ok;
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_log_emit(periapsis_component_log_entry_t *entry, periapsis_component_log_log_error_t *err) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  int32_t option;
  uint8_t * option1;
  size_t option2;
  if (((*entry).target).is_some) {
    const trail_host_string_t *payload0 = &((*entry).target).val;
    option = 1;
    option1 = (uint8_t *) (*payload0).ptr;
    option2 = (*payload0).len;
  } else {
    option = 0;
    option1 = 0;
    option2 = 0;
  }
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_log_emit((int32_t) (*entry).level, option, option1, option2, (uint8_t *) ((*entry).message).ptr, ((*entry).message).len, (uint8_t *) ((*entry).attributes).ptr, ((*entry).attributes).len, ptr);
  periapsis_component_log_result_void_log_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  if (!result.is_err) {
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_metrics_increment_counter(trail_host_string_t *name, uint64_t by, periapsis_component_metrics_labels_t *labels, periapsis_component_metrics_metric_error_t *err) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_metrics_increment_counter((uint8_t *) (*name).ptr, (*name).len, (int64_t) (by), (uint8_t *) ((*labels).values).ptr, ((*labels).values).len, ptr);
  periapsis_component_metrics_result_void_metric_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  if (!result.is_err) {
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_metrics_record_gauge(trail_host_string_t *name, double value, periapsis_component_metrics_labels_t *labels, periapsis_component_metrics_metric_error_t *err) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_metrics_record_gauge((uint8_t *) (*name).ptr, (*name).len, value, (uint8_t *) ((*labels).values).ptr, ((*labels).values).len, ptr);
  periapsis_component_metrics_result_void_metric_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  if (!result.is_err) {
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_metrics_record_histogram(trail_host_string_t *name, double value, periapsis_component_metrics_labels_t *labels, periapsis_component_metrics_metric_error_t *err) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_metrics_record_histogram((uint8_t *) (*name).ptr, (*name).len, value, (uint8_t *) ((*labels).values).ptr, ((*labels).values).len, ptr);
  periapsis_component_metrics_result_void_metric_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  if (!result.is_err) {
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_status_notify(periapsis_component_status_report_t *report, periapsis_component_status_status_error_t *err) {
  __attribute__((__aligned__(1)))
  uint8_t ret_area[2];
  int32_t option;
  uint8_t * option1;
  size_t option2;
  if (((*report).reason).is_some) {
    const trail_host_string_t *payload0 = &((*report).reason).val;
    option = 1;
    option1 = (uint8_t *) (*payload0).ptr;
    option2 = (*payload0).len;
  } else {
    option = 0;
    option1 = 0;
    option2 = 0;
  }
  int32_t option5;
  uint8_t * option6;
  size_t option7;
  if (((*report).message).is_some) {
    const trail_host_string_t *payload4 = &((*report).message).val;
    option5 = 1;
    option6 = (uint8_t *) (*payload4).ptr;
    option7 = (*payload4).len;
  } else {
    option5 = 0;
    option6 = 0;
    option7 = 0;
  }
  int32_t option10;
  int64_t option11;
  if (((*report).sequence).is_some) {
    const uint64_t *payload9 = &((*report).sequence).val;
    option10 = 1;
    option11 = (int64_t) (*payload9);
  } else {
    option10 = 0;
    option11 = 0;
  }
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_status_notify((uint8_t *) ((*report).component).ptr, ((*report).component).len, (uint8_t *) ((*report).instance).ptr, ((*report).instance).len, (int32_t) (*report).state, option, option1, option2, option5, option6, option7, (uint8_t *) ((*report).attributes).ptr, ((*report).attributes).len, option10, option11, ptr);
  periapsis_component_status_result_void_status_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      result.val.err = (int32_t) *((uint8_t*) (ptr + 1));
      break;
    }
  }
  if (!result.is_err) {
    return 1;
  } else {
    *err = result.val.err;
    return 0;
  }
}

bool periapsis_component_checkpoint_requested(void) {
  int32_t ret = __wasm_import_periapsis_component_checkpoint_requested();
  return ret;
}

void periapsis_component_checkpoint_save(trail_host_list_u8_t *state) {
  __wasm_import_periapsis_component_checkpoint_save((uint8_t *) (*state).ptr, (*state).len);
}

bool periapsis_component_checkpoint_load(trail_host_list_u8_t *ret) {
  __attribute__((__aligned__(sizeof(void*))))
  uint8_t ret_area[(3*sizeof(void*))];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_periapsis_component_checkpoint_load(ptr);
  trail_host_option_list_u8_t option;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      option.is_some = false;
      break;
    }
    case 1: {
      option.is_some = true;
      option.val = (trail_host_list_u8_t) { (uint8_t*)(*((uint8_t **) (ptr + sizeof(void*)))), (*((size_t*) (ptr + (2*sizeof(void*))))) };
      break;
    }
  }
  *ret = option.val;
  return option.is_some;
}

// Ensure that the *_component_type.o object is linked in

extern void __component_type_object_force_link_trail_host(void);
__attribute__((used))
void __component_type_object_force_link_trail_host_public_use_in_this_compilation_unit(void) {
  __component_type_object_force_link_trail_host();
}
