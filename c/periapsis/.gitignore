# Build artifacts. Source of truth: periapsis.c, the checked-in generated
# text bindings (generated/trail_host.{c,h}), and wit/. Object files - including
# the wit-bindgen component-type object - are regenerated by bindgen.sh / make.
*.o
libperiapsis.a
example/*.wasm
